server:
  port: 7000
  
spring:
  application:
    name: stock-market-api-gateway-service
   
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
    
management:
  endpoints:
    web:
      exposure:
        include: '*'

   
   
